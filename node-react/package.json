{
  "name": "root",
  "version": "0.0.0",
  "description": "A Node, GraphQL and React boilerplate for frontend interviews",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "npm-run-all -p -c dev:*",
    "dev:backend": "cd packages/backend && yarn dev",
    "dev:frontend": "cd packages/frontend && yarn start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
